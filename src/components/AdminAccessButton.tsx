
import React from 'react';

// This component is deprecated and no longer used
// Admin access is now handled through direct URL navigation to /admin
const AdminAccessButton = () => {
  return null;
};

export default AdminAccessButton;
